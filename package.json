{
  "name": "topnotchcarpentry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "npm":"6.4.1",
    "node":"11.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
